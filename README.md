# Google Images (Barry Pack)

Search Google Images via the Serper API.

## Tools

- `google_images_search` — search Google Images and return URLs, dimensions, and metadata
- `google_images_status` — check whether the Serper API key is configured
